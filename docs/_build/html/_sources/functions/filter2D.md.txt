# Two-dimensional filters

```r
filter2D(x, type = c("median3x3", "adimpro", "smooth"), ..., track = TRUE)

## S4 method for signature 'GPR'
filter2D(x, type = c("median3x3", "adimpro", "smooth"), ..., track = TRUE)
```

Two-dimensional filters