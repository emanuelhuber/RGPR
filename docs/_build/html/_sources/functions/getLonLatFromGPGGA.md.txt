# Get longitude and latitude from GPGGA sentence information (NMEA)

```r
getLonLatFromGPGGA(a)
```

## Arguments

- `a`: (`data.frame`) GPGGA sentence information

## Returns

(`data.frame`) Columns = latitude, longitude, elevation and time

Get longitude and latitude from GPGGA sentence information (NMEA)