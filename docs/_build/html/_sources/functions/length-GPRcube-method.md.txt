# Return length of a GPRcube/GPRslice object (number of slices)

```r
## S4 method for signature 'GPRcube'
length(x)
```

## Arguments

- `x`: (`GPR`)

Return length of a GPRcube/GPRslice object (number of slices)