# Closest trace

```r
findClosestCoord(x, y)
```

## Arguments

- `x`: Object of the class GPR.
- `y`: Length-two numeric vector of the (x, y)-coordinates.

## Returns

Indice (integer) of the closest trace.

Return the indice of the closest trace to the point `y`