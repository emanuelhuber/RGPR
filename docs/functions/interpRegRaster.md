# Interpolate raster to regular spacing

```r
interpRegRaster(
  vx,
  vy,
  z,
  nx,
  ny,
  method = c("linear", "nearest", "pchip", "cubic", "spline"),
  xy = c(TRUE, TRUE)
)
```

Interpolate raster to regular spacing