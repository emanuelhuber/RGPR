# Relative positions along path

```r
relPos(x)

## S4 method for signature 'GPR'
relPos(x)
```

## Arguments

- `x`: (`GPR|GPRsurvey`) An object

## Returns

(`numeric[m]`) Relative trace position along GPR line.

Returns the relative positions.