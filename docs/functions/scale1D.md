# Scale the traces

```r
scale1D(
  x,
  type = c("stat", "min-max", "95", "eq", "sum", "rms", "mad", "invNormal"),
  track = TRUE
)

## S4 method for signature 'GPR'
scale1D(
  x,
  type = c("stat", "min-max", "95", "eq", "sum", "rms", "mad", "invNormal"),
  track = TRUE
)
```

Scale the traces