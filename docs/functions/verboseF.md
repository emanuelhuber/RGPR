# Suppressing output from cat(), warnings & messages in functions

```r
verboseF(g, verbose = TRUE)
```

## Arguments

- `g`: `function` A function.
- `verbose`: (`logical[1]`) If `FALSE`, suppress any warnings and messages in function `g`, else do nothing.

Suppressing output from cat(), warnings & messages in functions