\name{exportPDF}
\alias{exportPDF}
\alias{exportPDF-methods}
\alias{exportPDF,GPR-method}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
	Export a PDF from the GPR profile
}
\description{
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
}
\usage{
exportPDF(x, filepath = NULL, add_topo = FALSE, clip = NULL, normalize = NULL, upsample = NULL, ...)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{x}{
%%     ~~Describe \code{x} here~~
}
  \item{filepath}{
%%     ~~Describe \code{filepath} here~~
}
  \item{add_topo}{
%%     ~~Describe \code{add_topo} here~~
}
  \item{clip}{
%%     ~~Describe \code{clip} here~~
}
  \item{normalize}{
%%     ~~Describe \code{normalize} here~~
}
  \item{upsample}{
%%     ~~Describe \code{upsample} here~~
}
  \item{\dots}{
%%     ~~Describe \code{\dots} here~~
}
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
%%  ~Describe the value returned
%%  If it is a LIST, use
%%  \item{comp1 }{Description of 'comp1'}
%%  \item{comp2 }{Description of 'comp2'}
%% ...
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
%%  ~~who you are~~
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{
##---- Should be DIRECTLY executable !! ----
##-- ==>  Define data, use random,
##--	or do  help(data=index)  for the standard data sets.

## The function is currently defined as
structure(function (x, filepath = NULL, add_topo = FALSE, clip = NULL, 
    normalize = NULL, upsample = NULL, ...) 
standardGeneric("exportPDF"), generic = structure("exportPDF", package = "RGPR"), package = "RGPR", group = list(), valueClass = character(0), signature = c("x", 
"filepath", "add_topo", "clip", "normalize", "upsample"), default = `\001NULL\001`, skeleton = (function (x, 
    filepath = NULL, add_topo = FALSE, clip = NULL, normalize = NULL, 
    upsample = NULL, ...) 
stop("invalid call in method dispatch to 'exportPDF' (no default method)", 
    domain = NA))(x, filepath, add_topo, clip, normalize, upsample, 
    ...), class = structure("standardGeneric", package = "methods"))
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
