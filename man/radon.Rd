% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radon.R
\name{radon}
\alias{radon}
\title{Compute the Radon Transform of a 2D Image}
\usage{
radon(
  image_matrix,
  n_theta = 180,
  n_rho = 0,
  delta_x = 1,
  theta_min = 0,
  theta_max = pi,
  linear_interp = TRUE,
  normalization = c("PET", "none", "skimage")
)
}
\arguments{
\item{image_matrix}{Numeric matrix representing the image to be transformed.}

\item{n_theta}{Integer, number of projection angles (default 180).}

\item{n_rho}{Integer, number of distance bins (rho) in the sinogram (default 0, auto).}

\item{delta_x}{Numeric, spacing of pixels in the image coordinate system (default 1.0).}

\item{theta_min}{Numeric, minimum angle in radians (default 0).}

\item{theta_max}{Numeric, maximum angle in radians (default pi).}

\item{linear_interp}{Logical, if TRUE use linear interpolation along rho (default TRUE).}

\item{normalization}{Character, type of normalization: 'none', 'PET', or 'skimage' (default 'PET').}
}
\value{
Numeric matrix: the Radon transform (sinogram) with rows corresponding to rho bins and columns to angles.
}
\description{
This function is an R wrapper for the C++ implementation \code{radon_transform_rcpp}.
It computes the discrete Radon transform (sinogram) of a given 2D numeric image.
}
\examples{
# Generate a simple test image
n <- 64
img <- matrix(0, n, n)
mid <- n/2
img[mid, ] <- 1
img[, mid] <- 1

# Compute sinogram
sino <- radon_wrapper(img, n_theta = 180, n_rho = 90)
image(t(sino[nrow(sino):1, ]), col = gray.colors(256), main = 'Sinogram')

}
