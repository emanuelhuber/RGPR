% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/z_readGPR.R
\name{register_gpr_format}
\alias{register_gpr_format}
\title{Register a GPR file format}
\usage{
register_gpr_format(
  id,
  detect_ext,
  mandatory,
  optional = character(0),
  gps_ext = NULL,
  reader_fn
)
}
\arguments{
\item{id}{(\code{character(1)}) Unique format identifier, e.g. \code{"DT1"}.}

\item{detect_ext}{(\code{character}) One or more uppercase extensions that
identify this format (e.g. \code{c("SGY", "SEGY")}).}

\item{mandatory}{(\code{character}) Named vector of required extensions,
slot name as names, e.g.
\code{c(DT1 = "DT1", HD = "HD")}.
The first element is the primary file.}

\item{optional}{(\code{character}) Named vector of optional extensions.
Defaults to \code{character(0)}.}

\item{gps_ext}{(\code{character(1)} or \code{NULL}) Name of the optional slot
whose resolved path is the GPS companion file.
\code{NULL} if the format has no GPS file or handles GPS
internally.}

\item{reader_fn}{(\code{function}) Format-specific reader.  Signature:
\verb{function(dsn, fName, fPath, desc, Vmax, verbose, ...)}.}
}
\description{
Called once per format (typically at the bottom of each \verb{io-*.R} file) to
add a format descriptor to the package-level registry.
}
\keyword{internal}
